endpoint: __ENTER YOUR ENDPOINT HERE - GET IT ON APP.PRISMA.IO__
datamodel: datamodel.prisma
seed:
  import: seed.zip

secret: ${env:PRISMA_SECRET}

generate:
  - generator: javascript-client
    output: ./generated/prisma-client/